# Heltec V3 WiFi TCP Bridge - Configuration
# Copy this to config.env and customize

# ===== WiFi Credentials =====
# Required: Your local WiFi SSID (2.4GHz only)
WIFI_SSID="YourNetwork"

# Required: Your WiFi password (WPA2 recommended)
WIFI_PASSWORD="YourPassword"

# Optional: TCP bridge port (default 5002)
TCP_PORT=5002

# Optional: Enable WiFi debug logging (0=off, 1=on)
WIFI_DEBUG_LOGGING=1

# ===== LoRa Radio Configuration =====
# Center frequency in MHz (869.618 for EU, 915 for US)
LORA_FREQ=869.618

# Bandwidth in kHz (62.5, 125, 250)
LORA_BW=62.5

# Spreading Factor (7-12, higher = longer range but slower)
LORA_SF=8

# Coding Rate (5-8)
LORA_CR=5

# TX Power in dBm (20-22 typical, max depends on region)
LORA_TX_POWER=22

# ===== Memory/Buffers =====
# Max number of contacts
MAX_CONTACTS=350

# Max group channels
MAX_GROUP_CHANNELS=40

# Offline message queue size
OFFLINE_QUEUE_SIZE=256

# Max unread messages
MAX_UNREAD_MSGS=32

# Max blob records
MAX_BLOBRECS=100

# ===== Display Configuration =====
# Display class (SSD1306Display for Heltec V3 OLED)
DISPLAY_CLASS=SSD1306Display

# Auto-off timeout in milliseconds (0 = never)
AUTO_OFF_MILLIS=15000

# UI recent list size
UI_RECENT_LIST_SIZE=4

# ===== Debug Flags (0=off, 1=on) =====
MESH_PACKET_LOGGING=1
MESH_DEBUG=1
BRIDGE_DEBUG=0
BLE_DEBUG_LOGGING=0

# ===== Node Identity =====
# Advertised node name on the mesh
ADVERT_NAME="Heltec V3 WiFi"

# GPS location (if broadcasting)
ADVERT_LAT=0.0
ADVERT_LON=0.0

# Repeater admin password (only needed for --repeater role)
ADMIN_PASSWORD="password"

# Repeater guest password (only needed for --repeater role)
GUEST_PASSWORD="guest"

# ===== Build Role =====
# companion or repeater
BUILD_ROLE="companion"

# ===== Upload Port =====
# Leave empty for auto-detect, or specify e.g. /dev/cu.usbmodem14201
UPLOAD_PORT=""

# Upload speed (helps if flashing is flaky at higher baud)
UPLOAD_SPEED=115200

# ===== Advanced =====
# Optional: custom build directory (default: ./build)
# WORK_DIR="/path/to/workdir"

# Optional: custom MeshCore repository URL
# REPO_URL="https://github.com/meshcore-dev/MeshCore"
# REPO_BRANCH="main"
